import { HeadingTitleDirective } from './heading-title.directive';

describe('HeadingTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingTitleDirective();
    expect(directive).toBeTruthy();
  });
});
