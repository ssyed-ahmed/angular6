import { HeroCardDirective } from './hero-card.directive';

describe('HeroCardDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroCardDirective();
    expect(directive).toBeTruthy();
  });
});
